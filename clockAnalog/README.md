# Analog Clock for Linux Mint
cd analog_clock
cargo run                  # debug build (fast compile, slower runtime)
cargo run --release        # optimized build (recommended for daily use)
cargo install --path .
analog_clock               # run from anywhere
